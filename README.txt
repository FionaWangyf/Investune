1.First install all the required libraries
2.Use python version 3.10 and below
3.Open two command lines, first run python flask_app.py to run flask, then in the second command line run streamlit run main.py